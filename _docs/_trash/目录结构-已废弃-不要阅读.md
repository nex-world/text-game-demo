
text-game-demo/
├─ package.json
├─ vite.config.ts
├─ tsconfig.json
├─ README.md
├─ public/
├─ src/
│  ├─ main.tsx                 # React entry
│  ├─ App.tsx
│  ├─ index.css                # tailwind
│  ├─ components/
│  │  ├─ ui/                   # shadcn 组件
│  │  ├─ Layout.tsx
│  │  ├─ StatusPanel.tsx
│  │  ├─ HistoryPanel.tsx
│  │  ├─ CurrentInfoPanel.tsx
│  │  └─ ActionPanel.tsx
│  ├─ store/                   # Zustand store（UI 层）
│  │  ├─ index.ts
│  │  ├─ slices/
│  │  │  └─ .......ts
│  │  └─ selectors.ts
│  │
│  ├─ game/                    # 具体游戏配置（可换皮/可替换）
│  │  ├─ settings.json         # 游戏设置 比如难度、初始资源等
│  │  ├─ place-templates.json  # 地点模板
│  │  ├─ places.json           # 预设的具体地点
│  │  ├─ actor-templates.json  # 角色模板
│  │  ├─ actors.json           # 预设的具体角色
│  │  ├─ property-defs.json    # 属性定义
│  │  ├─ item-defs.json        # 物品定义
│  │  ├─ facility-defs.json    # 设施定义
│  │  ├─ building-defs.json    # 建筑定义
│  │  ├─ event-templates.json  # 事件模板
│  │  ├─ events.json           # 预设的具体事件
│  │  └─ action-defs.json      # 动作定义
│  │
│  ├─ engine/                  # 核心引擎：无 UI、纯逻辑，可单元测试
│  │  ├─ index.ts              # Engine factory / bootstrap
│  │  ├─ types/                # 全局 TS 类型/接口
│  │  │  ├─ core.ts            # GameState, TimeState 等
│  │  │  ├─ time.ts            # 时间相关类型
│  │  │  ├─ place.ts           # 地图相关类型
│  │  │  ├─ actor.ts           # 角色相关类型
│  │  │  ├─ property.ts        # 属性相关类型（包括数值属性、人物关系、剧情状态、背包装备等）
│  │  │  ├─ item.ts            # 物品相关类型
│  │  │  ├─ facility.ts        # 设施相关类型
│  │  │  ├─ building.ts        # 建筑相关类型
│  │  │  ├─ event.ts           # 事件相关类型
│  │  │  └─ action.ts          # 动作相关类型
│  │  │
│  │  ├─ classes/              # 类
│  │  │  ├─ Engine/            # Engine class (tick, applyAction, scheduleEvent)
│  │  │  ├─ Scheduler/         # 调度器：事件调度/时间推进/队列
│  │  │  ├─ MapModel/          # 地图/地点/路径/地区/地域模型
│  │  │  │
│  │  │  ├─ Time/              # 时间转换/周期/时段计算
│  │  │  ├─ Place/             # 世界容器，注册地区/地点/路径
│  │  │  ├─ Actor/             # 玩家、NPC、牲畜、野兽、怪物、宠物、坐骑、召唤物等等
│  │  │  ├─ Property/          # 角色属性（力量、敏捷、智力等）、人物关系等其他属性（宿敌、师父等）
│  │  │  ├─ Item/              # 战斗装备、数值消耗品、便携交通工具、功能道具等等
│  │  │  ├─ Facility/          # 固定在特定地点或路径使用的固定设施（中小型）、公共交通工具、自然资源（植物、农作物、矿物等）之类
│  │  │  ├─ Building/          # 与地点直接关联的土地、建筑、大型固定设施之类
│  │  │  ├─ Event/             # 事件实例/定义/触发/解析
│  │  │  ├─ Action/            # 可执行 Action 的解析执行
│  │  │  │
│  │  │  ├─ ConfigLoader/      # 加载游戏配置（使用 Zod 校验）
│  │  │  └─ SaveManager/       # 管理游戏存档（使用 Zod 校验）
│  │  │
│  │  └─ utils/                # 引擎内部工具函数
│  │     ├─ serializers/       # 序列化/反序列化
│  │     ├─ ......             # 其他
│  │     └─ index.ts           # 工具函数入口
│  │
│  ├─ types/                   # 项目通用类型定义
│  ├─ utils/                   # 项目通用工具函数
│  └─ tests/                   # 单元测试（Vitest）
└─ _docs/
   └─ design.md                # 设计文档



